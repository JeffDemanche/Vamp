# URL of the Vamp GraphQL API.
# In development the client and API run on separate ports, so point at the API
# directly. Leave this unset in production builds: the client then defaults to a
# same-origin "/graphql" because the server serves both the SPA and the API.
VITE_GRAPHQL_URI=http://localhost:4000/graphql
