# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

input CreateProjectInput {
  contributorIds: [ID!]! = []
  ownerId: ID!
  title: String!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.This scalar is serialized to a string in ISO 8601 format and parsed from a string in ISO 8601 format.
"""
scalar DateTimeISO

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  createEmptyProject(ownerId: ID!): Project!
  createProject(input: CreateProjectInput!): Project!
  login(input: LoginInput!): User!
  logout: Boolean!
  register(input: RegisterInput!): User!
  setProjectArchived(archived: Boolean!, id: ID!): Project!
  updateProjectMetadata(input: UpdateProjectMetadataInput!): Project!
}

type Project {
  _id: ID!
  archived: Boolean!
  contributors: [User!]!
  createdAt: DateTimeISO!
  owner: User!
  projectData: ProjectData!
  title: String!
}

type ProjectClip {
  _id: ID!
  createdAt: DateTimeISO!
  duration: Int!
  start: Int!
  track: ID!
}

type ProjectData {
  _id: ID!
  clips: [ProjectClip!]!
  createdAt: DateTimeISO!
  tracks: [ProjectTrack!]!
}

type ProjectTrack {
  _id: ID!
  createdAt: DateTimeISO!
  name: String!
}

type Query {
  me: User
  project(id: ID!): Project
  projectsByUser(includeArchived: Boolean! = false, userId: ID!): [Project!]!
  user(id: ID!): User
  userByEmail(email: String!): User
  users: [User!]!
}

input RegisterInput {
  email: String!
  password: String!
  username: String!
}

input UpdateProjectMetadataInput {
  id: ID!
  title: String
}

type User {
  _id: ID!
  createdAt: DateTimeISO!
  email: String!
  username: String!
}