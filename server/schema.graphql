# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

input ArchiveClipsInput {
  clipIds: [ID!]!
  projectId: ID!
}

"""Upload lifecycle of a ProjectAudio's underlying S3 object."""
enum AudioUploadStatus {
  PENDING
  READY
}

"""How a ProjectClip schedules its underlying audio for playback."""
enum ClipMode {
  FLAT
  STACKED
}

input CreateAudioUploadInput {
  contentType: String!
  filename: String
  loopLength: Int
  projectId: ID!
}

type CreateAudioUploadPayload {
  audio: ProjectAudio!
  uploadUrl: String!
}

input CreateClipInput {
  audioId: ID!
  audioOffset: Int! = 0
  duration: Int!
  mode: ClipMode! = FLAT
  projectId: ID!
  start: Int!
  trackId: ID!
}

input CreateProjectInput {
  contributorIds: [ID!]! = []
  ownerId: ID!
  title: String!
}

input CreateTrackInput {
  name: String
  projectId: ID!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.This scalar is serialized to a string in ISO 8601 format and parsed from a string in ISO 8601 format.
"""
scalar DateTimeISO

input DeleteTrackInput {
  projectId: ID!
  trackId: ID!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  archiveClips(input: ArchiveClipsInput!): ProjectData!
  createAudioUpload(input: CreateAudioUploadInput!): CreateAudioUploadPayload!
  createClip(input: CreateClipInput!): ProjectClip!
  createEmptyProject(ownerId: ID!): Project!
  createProject(input: CreateProjectInput!): Project!
  createTrack(input: CreateTrackInput!): ProjectData!
  deleteTrack(input: DeleteTrackInput!): ProjectData!
  login(input: LoginInput!): User!
  logout: Boolean!
  register(input: RegisterInput!): User!
  setProjectArchived(archived: Boolean!, id: ID!): Project!
  updateClip(input: UpdateClipInput!): ProjectClip!
  updateProjectMetadata(input: UpdateProjectMetadataInput!): Project!
  updateProjectUserState(input: UpdateProjectUserStateInput!): ProjectUser!
}

type Project {
  _id: ID!
  archived: Boolean!
  contributors: [ProjectUser!]!
  createdAt: DateTimeISO!
  owner: ProjectUser!
  projectData: ProjectData!
  title: String!
}

type ProjectAudio {
  _id: ID!
  bucket: String!
  byteSize: Int
  contentType: String!
  createdAt: DateTimeISO!
  downloadUrl: String
  filename: String
  key: String!
  loopLength: Int
  uploadStatus: AudioUploadStatus!
}

type ProjectClip {
  _id: ID!
  audio: ProjectAudio!
  audioOffset: Int!
  createdAt: DateTimeISO!
  duration: Int!
  maxDuration: Int!
  mode: ClipMode!
  start: Int!
  track: ID!
}

type ProjectData {
  _id: ID!
  audios: [ProjectAudio!]!
  clips: [ProjectClip!]!
  createdAt: DateTimeISO!
  tracks: [ProjectTrack!]!
}

type ProjectTrack {
  _id: ID!
  createdAt: DateTimeISO!
  name: String!
}

type ProjectUser {
  _id: ID!
  createdAt: DateTimeISO!
  loop: Boolean!
  playEnd: Int!
  playStart: Int!
  recording: ProjectUserRecording
  selectedTrack: ID
  user: User!
  viewportEnd: Int!
  viewportStart: Int!
}

type ProjectUserRecording {
  startSample: Int!
  startedAt: DateTimeISO!
  track: ID!
}

input ProjectUserRecordingInput {
  startSample: Int!
  startedAt: DateTimeISO!
  track: ID!
}

type Query {
  me: User
  project(id: ID!): Project
  projectAudio(id: ID!): ProjectAudio
  projectUser(projectId: ID!): ProjectUser
  projectsByUser(includeArchived: Boolean! = false, userId: ID!): [Project!]!
  user(id: ID!): User
  userByEmail(email: String!): User
  users: [User!]!
}

input RegisterInput {
  email: String!
  password: String!
  username: String!
}

input UpdateClipInput {
  clipId: ID!
  duration: Int
  projectId: ID!
  start: Int
  track: ID
}

input UpdateProjectMetadataInput {
  id: ID!
  title: String
}

input UpdateProjectUserStateInput {
  loop: Boolean
  playEnd: Int
  playStart: Int
  projectId: ID!
  recording: ProjectUserRecordingInput
  selectedTrack: ID
  viewportEnd: Int
  viewportStart: Int
}

type User {
  _id: ID!
  createdAt: DateTimeISO!
  email: String!
  username: String!
}